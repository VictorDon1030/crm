<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wolfcode.crm.mapper.PayMapper">
  <resultMap id="BaseResultMap" type="cn.wolfcode.crm.domain.Pay">
    <id column="id" property="id" />
    <result column="amount" property="amount" />
    <result column="date" property="date" />
    <result column="remark" property="remark" />
    <result column="type" property="type" />
  </resultMap>
  <delete id="deleteByPrimaryKey">
    delete from pay
    where id = #{id}
  </delete>
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
    insert into pay (amount, date, remark,type)
    values (#{amount}, #{date}, #{remark},#{type})
  </insert>
  <update id="updateByPrimaryKey">
    update pay
    set amount = #{amount},
      date = #{date},
      remark = #{remark},
      type=#{type}
    where id = #{id}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap">
    select id, amount, date, remark,type
    from pay
    where id = #{id}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, amount, date, remark,type
    from pay
  </select>
</mapper>